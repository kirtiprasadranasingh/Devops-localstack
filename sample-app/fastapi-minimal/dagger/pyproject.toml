[project]
name = "fastapi-minimal-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["dagger-io==0.20.8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/minimal_pipeline"]
